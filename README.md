# term

Web terminal built with Next.js + xterm.js.

The application lives in [`web-terminal-simulator/`](web-terminal-simulator/README.md) —
see that README for setup, environment variables, deployment, and the two
terminal modes (serverless virtual shell and real PTY backend).
